# toto教室自動化システム

冬季のフリースタイルモーグル教室（toto教室）向けに、Googleフォーム＋Google Apps Script＋Googleスプレッドシートで構成された申込自動化システム（申込→定員判定→参加確定/キャンセル待ち振り分け→自動確認メール送信→管理者向け通知）を構築・運用しているプロジェクト。

本番運用中の会場は3つ（手稲・さのさか・宮城）。詳細な会場情報・プロジェクトID・過去のインシデント記録などは、このフォルダの `handoff_*.md` およびCowork側のproject memory（`forms_reference.md`）を参照。
